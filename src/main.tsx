import React from 'react'
import ReactDOM from 'react-dom/client'
import IndexNewtab from '../newtab'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <IndexNewtab />
  </React.StrictMode>,
)